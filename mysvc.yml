apiVersion: v1
kind: Service
metadata:
  name: javaapp-svc
  labels:
    app: myjavaapp    
spec:
  type: NodePort
  ports: 
  - port: 8080
    nodePort: 30002
    protocol: TCP
  selector:
    app: myjavaapp 
    

