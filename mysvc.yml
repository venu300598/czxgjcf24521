apiVersion: v1
kind: Service
metadata:
  name: myjavaapp-svc
  labels:
    app: myjavaapp
spec:
  type: NodePort
  ports: 
  - port: 80
    nodePort: 30003
    protocol: TCP
  selector:
    app: myjavaapp  

